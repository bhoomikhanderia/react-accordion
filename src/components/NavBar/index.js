import React from 'react';
import Logo from '../../logo.png';

const NavBar = () => (
    <nav>
        <a className="navbar-brand" href="/"><img src={Logo} alt="aesop logo"/></a>
    </nav>
)

export default NavBar
